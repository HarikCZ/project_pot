/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: false,
}
module.exports = {
  images: {
    disableStaticImages: true
  }
}

module.exports = nextConfig
